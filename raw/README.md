# raw/

여기에 원본 소스를 넣는다. 위키가 읽는 immutable한 진실의 출처.

- 웹 클립(Obsidian Web Clipper 등)
- PDF / 논문
- 메모 / 트랜스크립트
- 이미지(`raw/assets/`)

LLM은 여기를 **읽기만** 한다. 절대 수정하지 않는다. 가공된 결과물은 모두 `wiki/`에 쓴다.

기본적으로 `raw/` 안의 파일은 git에서 추적하지 않는다(`.gitignore` 참조). 자기 vault에서 소스도 git으로 추적하고 싶다면 `.gitignore`를 수정한다.
